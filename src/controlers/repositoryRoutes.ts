
import { Request, Response } from 'express'


export const getRepositories = (req: Request, res: Response): void => {
  res.send('getRepositories route not implamented yet')
}

export const addRepository = (req: Request, res: Response): void => {
  res.send('addRepository route not implamented yet')
}

export const getRepository = (req: Request, res: Response): void => {
  res.send('getRepository route not implamented yet')
}

export const updateRepository = (req: Request, res: Response): void => {
  res.send('updateRepository route not implamented yet')
}

export const deleteRepository = (req: Request, res: Response): void => {
  res.send('deleteRepository route not implamented yet')
}

